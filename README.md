# urlencoder
Implements java land `URLEncoder` in nodejs

## Install
`$ npm install urlencoder`

## Example

Super simple:

```javascript
var encoder = require('urlencoder');
encoder.encode('哈哈');
```
